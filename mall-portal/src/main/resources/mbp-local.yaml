spring:
  config:
    import: classpath:mbp.yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/qdd_design_mall?useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root1121