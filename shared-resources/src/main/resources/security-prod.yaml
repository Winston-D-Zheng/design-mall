--- # 安全相关配置
security:
  ignoreUrls: >
    /favicon.ico,
    /actuator/**,
    /druid/**,
    /admin/user/login,
    /admin/user/register,
    /admin/user/sendSmsCode,
    /member/login,
    /member/register
jwt:
  #JWT的超期限时间(60*60*24*7)